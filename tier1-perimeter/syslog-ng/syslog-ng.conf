# =============================================================================
# SURU Platform — syslog-ng configuration (RETIRED REFERENCE — SEC-017)
# =============================================================================
# This file is a stale reference and MUST NOT be deployed to the router.
#
# The authoritative syslog-ng configuration for pfSense is the template:
#   tier1-perimeter/templates/pfsense/syslog-ng.conf.tpl
#
# The template is rendered and deployed to the router by:
#   make deploy  (in tier1-perimeter/)
#
# Known issues that make this file dangerous to deploy (SEC-017):
#   - No sni(yes): frontdoor SNI demux cannot route the connection;
#     syslog goes to the HTTPS relay instead of Logstash.
#   - No disk-buffer: events are lost during any SIEM outage.
#   - No ssl-options: TLS version and cipher suite are unconstrained.
#   - Output template emits empty {} JSON for the pfSense log path
#     (ECS field mapping is missing entirely).
#   - No .suru.log_type rewrite tags: Logstash routing matches nothing
#     and every event falls through to the dead-letter pipeline.
# =============================================================================
