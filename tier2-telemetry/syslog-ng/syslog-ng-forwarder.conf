# =============================================================================
# SURU Platform — tier2 syslog-ng forwarder config (RETIRED REFERENCE — SEC-016)
# =============================================================================
# [STUB: SEC-016] This file has known bugs that make it non-functional.
# DO NOT DEPLOY. See syslog-ng-pfsense.conf (same directory) for full context.
#
# Known bugs in the retired config (SEC-016):
#   (a) No disk-buffer — events are lost during any SIEM outage.
#   (b) Connects to port 5044 directly, bypassing the frontdoor SNI/SAN
#       contract; the Logstash container is not exposed to the LAN.
#   (c) No sni(yes) — frontdoor SNI demux cannot route the connection.
#   (d) No JSON template — ships default syslog format, not ECS-compatible.
#   (e) No .suru.log_type rewrite tags — Logstash routing matches nothing.
# =============================================================================
